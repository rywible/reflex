[alias]
xtask = "run --package xtask --"

# Burn's aarch64 f16 kernels contain FP16 instructions. Select the architectural
# extension directly without coupling builds to a provider-specific CPU model.
[target.'cfg(target_arch = "aarch64")']
rustflags = ["-C", "target-feature=+fp16"]
